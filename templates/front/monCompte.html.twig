{% extends 'front/layout.html.twig' %}

{% block body %}
 {# <section class="MonCompte">
  
  </section> #}
    {# {{ include('front/_layout.html.twig')}} #}

<div class="container ">
<div class="row ">
    <div class="col-12 col-sm-6 col-lg-6 ">
        <div class="compte">
            <a href="{{ path ("user_show", {"id": app.user.id })}}">Mon profil</a>
        </div>

        <div class="compte">
            <a href="{{ path ("user_edit", {"id": app.user.id })}}">Modifier mon compte</a>
        </div>

    </div>
    <div class="col-12 col-sm-6 col-lg-6">
        {% if is_granted ('ROLE_ADMIN') %}
        {% else %}
        <div class="compte">
            <a href="{{ path ("mes_reservations", {"id": app.user.id })}}">Consulter mes réservations</a>
        </div>
        <div class="compte">
            <a href="#">Consulter mes commandes</a>
        </div>
        {% endif %}
    </div>
</div>
</div>
{% endblock %}